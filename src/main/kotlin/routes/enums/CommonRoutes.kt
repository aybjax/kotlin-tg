package routes.enums

import com.google.gson.annotations.SerializedName
import com.squareup.moshi.JsonClass

enum class CommonRoutes: Routes {
    GREET_USER,
    ACCOUNT_PAGE,
    CHOOSE_PRODUCT,
    LOCATION,
}